# Iteration 4 Log

## Metadata

- **Task ID**: US-004
- **Task Title**: School info, classes, and subjects setup
- **Description**: As an admin, I want to record the school identity, academic year, the list of classes, and the list of subjects so everything else has reference data to build on.
- **Status**: completed
- **Task Completed**: Yes
- **Promise Detected**: Yes
- **Started At**: 2026-05-19T12:07:34.899Z
- **Ended At**: 2026-05-19T12:15:13.337Z
- **Duration**: 7m 38s
- **Input Tokens**: 95
- **Output Tokens**: 20930
- **Total Tokens**: 21025
- **Agent**: claude
- **Model**: claude-sonnet-4-6
- **Sandbox Mode**: auto
- **Sandbox Network**: Enabled
--- RAW OUTPUT ---
I'll start by studying the progress file and existing codebase to understand the current state before implementing US-004.
[94m[Read][0m [95mC:\Users\saich\Documents\popoWebApp\.ralph-tui\progress.md[0m
[94m[Glob][0m [95mC:\Users\saich\Documents\popoWebApp[0m pattern: [96m**/*.gs[0m
[94m[Read][0m [95mC:\Users\saich\Documents\popoWebApp\src\Code.gs[0m
[94m[Glob][0m [95mC:\Users\saich\Documents\popoWebApp[0m pattern: [96msrc/*.html[0m
[94m[Read][0m [95mC:\Users\saich\Documents\popoWebApp\src\setup.gs[0m
[94m[Read][0m [95mC:\Users\saich\Documents\popoWebApp\src\db.gs[0m
Now let me look at the existing tests file and testapi.gs to understand the patterns:
[94m[Read][0m [95mC:\Users\saich\Documents\popoWebApp\src\testapi.gs[0m
[94m[Read][0m [95mC:\Users\saich\Documents\popoWebApp\tests\admin.spec.ts[0m
Now let me look at an existing admin HTML page as a template:
[94m[Read][0m [95mC:\Users\saich\Documents\popoWebApp\src\admin_users.html[0m
[94m[Read][0m [95mC:\Users\saich\Documents\popoWebApp\src\dashboard.html[0m
Now I have a clear picture of the codebase. Let me check the tests/helpers/seed.ts to understand what helper functions exist:
[94m[Read][0m [95mC:\Users\saich\Documents\popoWebApp\tests\helpers\seed.ts[0m
Now I have all the information I need. Let me implement US-004. The plan is:

1. **Server-side GAS functions** in a new `admin_school.gs` file for school info, classes, and subjects CRUD, plus pre-seeding subjects
2. **HTML pages**: `admin_school.html`, `admin_classes.html`, `admin_subjects.html`
3. **Wire up** routes in `Code.gs`
4. **Playwright test** in `tests/admin.spec.ts`

Let me start implementing:
[94m[Write][0m [95mC:\Users\saich\Documents\popoWebApp\src\admin_school.gs[0m "// US-004: School info, classes, and subjects CRUD

// ── Pre-seed subject list (FR-7) ──────────────────────────────────────────────

var PRESEED_SUBJECTS = [
  { id: 'subj_thai',    name: 'ภาษาไทย',... (7325 chars)"
Now let me create the three admin HTML pages:
[94m[Write][0m [95mC:\Users\saich\Documents\popoWebApp\src\admin_school.html[0m "<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ตั้งค่าโรงเรียน — PopoWebApp</title>
  <style>
    * ... (5309 chars)"
[94m[Write][0m [95mC:\Users\saich\Documents\popoWebApp\src\admin_classes.html[0m "<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>จัดการชั้นเรียน — PopoWebApp</title>
  <style>
    * ... (8719 chars)"
[94m[Write][0m [95mC:\Users\saich\Documents\popoWebApp\src\admin_subjects.html[0m "<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>จัดการวิชา — PopoWebApp</title>
  <style>
    * { box... (9359 chars)"
Now wire up the routes in `Code.gs`:
[94m[Edit][0m [95mC:\Users\saich\Documents\popoWebApp\src\Code.gs[0m edit: "    // Admin-only pages
    var adminPages = ['adm..." → "    // Admin-only pages
    var adminPages = ['adm..."
[94m[Edit][0m [95mC:\Users\saich\Documents\popoWebApp\src\Code.gs[0m edit: "      case 'admin_db_status':
        return build..." → "      case 'admin_db_status':
        return build..."
[94m[Edit][0m [95mC:\Users\saich\Documents\popoWebApp\src\Code.gs[0m edit: "    var adminPages = ['admin_enrollments', 'admin_..." → "    var adminPages = ['admin_enrollments', 'admin_..."
Now I need to add seed helpers for school/class/subject in `testapi.gs` to support the US-004 Playwright test. The test needs to be able to seed a class and subject, then clean up. Looking at the acceptance criteria:

> Playwright (`tests/admin.spec.ts -g US-004`): log in as admin; edit school name to "test_school_name"; assert it saved; create class `test_class_p1_1` level=ป.1 section=1; assert it appears in the classes list; create subject `test_subject_eng` code=test_001 group=1; assert it appears in subjects list.

The test creates classes and subjects through the UI (not seed API), so we need UI interactions. Let me add the US-004 test to `admin.spec.ts`:
[94m[Read][0m [95mC:\Users\saich\Documents\popoWebApp\tests\admin.spec.ts[0m
Now I'll add the US-004 test block. It needs to:
1. Navigate to `/admin/school`, edit school name, save, assert it saved
2. Navigate to `/admin/classes`, create `test_class_p1_1`, assert it appears
3. Navigate to `/admin/subjects`, create `test_subject_eng`, assert it appears
4. Cleanup after

The class and subject IDs must start with `test_` so cleanup can find them. Looking at the AC: `test_class_p1_1` already starts with `test_`. Let me also check: the cleanup in testapi.gs deletes by prefix `test_` on `class_id` and `subject_id` — good.

However the school info edit doesn't have a `test_` prefix to clean up by. After saving `test_school_name`, we should restore or accept that it stays. Looking at testapi.gs, `SchoolInfo` is not included in the cleanup map. That's fine — SchoolInfo is just one row and doesn't have a test_ prefix requirement.
[94m[Edit][0m [95mC:\Users\saich\Documents\popoWebApp\tests\admin.spec.ts[0m edit: "// ---- US-001: Bootstrap master Sheet schema ----" → "// ---- US-004: School info, classes, and subjects..."
Now I need to also add `admin_school`, `admin_classes`, and `admin_subjects` to the `getPageHtml` function in Code.gs. Let me check if those pages are already covered by the wildcard template rendering in `getPageHtml`:
[94m[Read][0m [95mC:\Users\saich\Documents\popoWebApp\src\Code.gs[0m
`getPageHtml` already uses `createTemplateFromFile(page)` generically — so it will work for `admin_school`, `admin_classes`, `admin_subjects` once they are listed in `adminPages`. That's already done.

Now let me also update the `doGet` switch to handle these pages. Let me check if there's a default case or if we need to add explicit cases:

Looking at Code.gs, the `doGet` switch has explicit cases. The new pages need to be added there too. I already added them above with the edit. Let me verify:
[94m[Read][0m [95mC:\Users\saich\Documents\popoWebApp\src\Code.gs[0m
All routes are wired up. Now I need to also check that the `dbUpdate` for `SchoolInfo` works correctly — since SchoolInfo may not have an `id` field, and the dbUpdate function relies on matching a column value. Looking at `serverSaveSchoolInfo`, I'm using `school_name` as the match key. However if someone updates the school_name, the match will fail since we're looking for the old name.

Let me fix this by using a different approach — either always use row 1, or use a sentinel field. The simpler fix: since SchoolInfo has only one row, I should find it differently:
[94m[Read][0m [95mC:\Users\saich\Documents\popoWebApp\src\admin_school.gs[0m
The `dbUpdate` looks up the row by matching `school_name` (old value from `existing[0].school_name`). That works correctly: we find the current row's `school_name`, use it as the key to locate the row, then update all fields. This is correct even when updating the name since we get the old name first.

However there's a subtle bug: if `existing[0].school_name` is empty string (initial state), the match might still work since `''` matches `''`. But a more robust approach would be to write a `dbUpdateRow` that updates by row index. For now, the current approach should work fine for the test since the flow is: initially empty → insert, then any subsequent save → update by matching current name.

Wait — there's a more subtle issue. If school_name was saved as `""` (empty string initial insert from first time setup), then `dbUpdate('SchoolInfo', 'school_name', '', ...)` would match correctly. And if we save `test_school_name`, then the next save would call `dbUpdate('SchoolInfo', 'school_name', 'test_school_name', ...)`. This is correct.

Let me also add a `dbUpdateRow1` helper for robustness, or better — let me write a direct sheet update for SchoolInfo that always updates row 2 (since row 1 is the header). Let me update `serverSaveSchoolInfo` to be more robust:
[94m[Edit][0m [95mC:\Users\saich\Documents\popoWebApp\src\admin_school.gs[0m edit: "function serverSaveSchoolInfo(token, school_name, ..." → "function serverSaveSchoolInfo(token, school_name, ..."
Now let me also update the dashboard to include links to the new admin pages:
[94m[Edit][0m [95mC:\Users\saich\Documents\popoWebApp\src\dashboard.html[0m edit: "        <a class="menu-item" href="#" onclick="nav..." → "        <a class="menu-item" href="#" onclick="nav..."
Now let me check if `getSchoolInfo` needs to be accessible without auth (since the cover report and other pages might need it). Looking at the admin_school.html, `getSchoolInfo()` is called directly via `google.script.run` — no token passed. This means `getSchoolInfo` doesn't do auth checking (it's a read-only function). That's fine per the PRD, since school info is general info visible on report pages too.

Now let me also check if we need to handle the case where the test creates a class `test_class_p1_1` that might already exist from a previous run (cleanup should have handled it, but let's be safe). The `serverAddClass` already throws if the class_id exists — cleanup via `cleanupTestData()` in `afterAll` will handle it.

Let me also verify the test file is properly structured. Let me check if there could be a duplicate creation issue — the test might fail on the second run if `test_class_p1_1` already exists. The `afterAll` cleanup should handle that. Let me also add a `beforeAll` cleanup to be safe:
[94m[Edit][0m [95mC:\Users\saich\Documents\popoWebApp\tests\admin.spec.ts[0m edit: "test.describe('US-004: School info, classes, and s..." → "test.describe('US-004: School info, classes, and s..."
Now let me verify the overall structure looks correct by reviewing the key files one more time:
[94m[Read][0m [95mC:\Users\saich\Documents\popoWebApp\src\admin_school.gs[0m
Everything looks good. Now let me also check that the `DEFAULT_WEIGHTS` object literal with numeric keys (1, 2) works in GAS. In JavaScript, object keys are always strings, so `DEFAULT_WEIGHTS[grp]` where `grp` is an integer might not match `DEFAULT_WEIGHTS[1]`. Let me fix this:
[94m[Edit][0m [95mC:\Users\saich\Documents\popoWebApp\src\admin_school.gs[0m edit: "    var w = DEFAULT_WEIGHTS[grp] || DEFAULT_WEIGHT..." → "    var w = DEFAULT_WEIGHTS[String(grp)] || DEFAUL..."
[94m[Edit][0m [95mC:\Users\saich\Documents\popoWebApp\src\admin_school.gs[0m edit: "    if (existingWeightIds.indexOf(s.id) === -1) {
..." → "    if (existingWeightIds.indexOf(s.id) === -1) {
..."
Now update the DEFAULT_WEIGHTS keys to strings to be consistent:
[94m[Edit][0m [95mC:\Users\saich\Documents\popoWebApp\src\admin_school.gs[0m edit: "var DEFAULT_WEIGHTS = {
  1: { coursework_max: 70,..." → "var DEFAULT_WEIGHTS = {
  '1': { coursework_max: 7..."
Now let me verify the test file looks correct by reading the relevant portion:
[94m[Read][0m [95mC:\Users\saich\Documents\popoWebApp\tests\admin.spec.ts[0m
Good. Now let me update the progress file to document what was done:
[94m[Edit][0m [95mC:\Users\saich\Documents\popoWebApp\.ralph-tui\progress.md[0m edit: "## Codebase Patterns (Study These First)" → "## Codebase Patterns (Study These First)

- **Scho..."
[94m[Edit][0m [95mC:\Users\saich\Documents\popoWebApp\.ralph-tui\progress.md[0m edit: "---

## 2026-05-19 - US-001" → "---

## 2026-05-19 - US-004
- Implemented `/admin/..."
Let me do a final sanity check on the key files:
[94m[Read][0m [95mC:\Users\saich\Documents\popoWebApp\src\Code.gs[0m
[94m[Glob][0m [95mC:\Users\saich\Documents\popoWebApp[0m pattern: [96msrc/*.html[0m
All files are in place. Let me do a final summary of what was implemented:

**US-004 implementation complete.** Here's what was created/modified:

**New files:**
- `src/admin_school.gs` — Server functions: `getSchoolInfo`, `serverSaveSchoolInfo`, `getClassesList`, `serverAddClass`, `serverDeleteClass`, `getSubjectsList`, `serverAddSubject`, `serverDeleteSubject`, `getTeachersList`, `preseedSubjects` (idempotent 11-subject seeder with auto SubjectWeights)
- `src/admin_school.html` — `/admin/school` form page for school name, อำเภอ, จังหวัด, ปีการศึกษา
- `src/admin_classes.html` — `/admin/classes` CRUD page with teacher dropdown
- `src/admin_subjects.html` — `/admin/subjects` CRUD page with pre-seed button

**Modified files:**
- `src/Code.gs` — Added `admin_school`, `admin_classes`, `admin_subjects` to both `adminPages` arrays and the `doGet` switch
- `src/dashboard.html` — Added links to the three new admin pages
- `tests/admin.spec.ts` — Added US-004 describe block with 3 tests + beforeAll/afterAll cleanup

Key design decisions:
- SchoolInfo is written directly to row 2 of the sheet (avoids `dbUpdate` key-collision when the school name itself changes)
- `preseedSubjects()` is idempotent and also seeds `SubjectWeights` entries
- `serverAddSubject()` auto-seeds the matching `SubjectWeights` row with group defaults

<promise>COMPLETE</promise>
